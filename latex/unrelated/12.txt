; This example illustrates basic arithmetic and
; uninterpreted functions

(declare-fun a () Int)
(declare-fun b () Int)
(assert (= (mod a 2) 0))
(assert (> (+ a b) -3))
(assert (> 6 a))
(assert (= (+ a (* 2 b)) 11))
(check-sat)
(get-model)

unsat Z3(11, 10): ERROR: model is not available